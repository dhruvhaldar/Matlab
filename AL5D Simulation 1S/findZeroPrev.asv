function t = findZeroPrev(fun,tvec,index)
    ti = tvec(index);
    while (sign(fun(ti)) == sign(fun(ti)))
        index = index - 1;
        
        if(dr*(rtest-ri) < 0 ) 
            skip = 1;
            break;
        end
    end
    
    if (skip~=1)
        r = fzero(fun,[rtest,R]);    
    else
        r = NaN;
    end
end